#ifndef COMMON_H
#define COMMON_H

/*
 * COMMON TYPES
 */
#include <esp_err.h>
#include <stdbool.h>
typedef unsigned int uint;

/*
 * COMMON MACROS
 */
#define ARRAY_SIZE(x) (sizeof(x) / sizeof((x)[0]))

#endif
